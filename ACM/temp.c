
#include <stdlib.h>


int main() {
	while(malloc(1);
    )
	// ErrorCode ec = success;
	// multiset<pair<int, int>> ms;
	// ms.insert(make_pair(1, 3));
	// ms.insert(make_pair(1, 2));
	// ms.insert(make_pair(2, 2));
	// ms.insert(make_pair(2, 3));
	// multiset<pair<int, int>>::iterator toerase;
	// int smallest = 100000;
	// for (auto it = ms.begin(); it != ms.end(); it++) {
	// 	if(it->first == 1){
	// 		if(it->second < smallest){
	// 			toerase = it;
	// 			smallest = it->second;
	// 		}
	// 	}
	// }
	// ms.erase(toerase);
	// int row = 100000, col = 100000;
	// cin >> row >> col; // 输入行列
	// int **map = new int *[row + 2]; // 动态初始化行
	// for (int i = 0; i < row + 2;i++) {
	// 	map[i] = new int[col + 2]; // 动态初始化列
	// }

	return 0;
}