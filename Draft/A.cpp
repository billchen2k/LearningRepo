#include <stdio.h>
#include <time.h>
int main(void) {
	// float f = 0, celsius = 0;
	// scanf("%f", &f);
	// celsius = f * 5.0 / 9.0 - (32 * 5 / 9);
	// // celsius = (f / 9 - 32 / 9) * 5;
	// printf("celsius=%d\n", (int)celsius);
	// return 0;
	printf("Current time:%ld\n", time(NULL));
	
}